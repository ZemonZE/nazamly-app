meta {
  name: Delete Folder (Admin)
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/admin/materials/folders/:folderId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  folderId: REPLACE_WITH_FOLDER_ID
}

docs {
  # [Admin] Delete Materials Folder
  Requires admin role.
}
