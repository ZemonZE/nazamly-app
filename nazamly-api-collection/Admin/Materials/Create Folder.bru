meta {
  name: Create Folder (Admin)
  type: http
  seq: 1
}

post {
  url: {{base_url}}/admin/materials/folders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "name": "Lecture Notes",
    "courseInstanceId": "REPLACE_WITH_COURSE_INSTANCE_ID"
  }
}

docs {
  # [Admin] Create Materials Folder
  Requires admin role.
}
