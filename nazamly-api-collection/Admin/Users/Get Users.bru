meta {
  name: Get Users
  type: http
  seq: 1
}

get {
  url: {{base_url}}/admin/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

docs {
  # [Admin] Get Users
  Requires admin role. Returns all registered users.
}
