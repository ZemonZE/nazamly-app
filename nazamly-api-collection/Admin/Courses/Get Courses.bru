meta {
  name: Get Courses
  type: http
  seq: 1
}

get {
  url: {{base_url}}/admin/courses
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

docs {
  # [Admin] Get Courses
  Requires admin role. Returns all courses.
}
