meta {
  name: Get All Courses
  type: http
  seq: 1
}

get {
  url: {{base_url}}/courses
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

docs {
  # Get All Courses
  
  Returns all available courses in the system.
}
